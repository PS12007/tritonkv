# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/

# Environments
.venv/
venv/
env/

# Benchmark artifacts (regenerate with `python benchmark.py`)
# The whole directory, not just its top level: `benchmark.py --out` writes into
# results/runs/ for the between-run comparison, and the glob patterns this used
# to list matched one level only, so those runs and their logs were untracked
# but not ignored.
results/

# OS / editor
.DS_Store
Thumbs.db
*.swp

# Claude Code local settings
.claude/settings.local.json

# Personal drafting doc, not for publication
docs/x_posts.md
